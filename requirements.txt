numpy~=2.2
shapely~=2.0
deprecated~=1.2.18
# jupedsim_visualizer runtime deps (separate distribution, needed to run
# it from the checkout and for its tests)
pyside6~=6.8
vtk~=9.4
# build deps
wheel
build
setuptools

# test deps
pytest~=9.1
pandas~=2.2

# ci deps
jinja2~=3.1
ruff~=0.14
pre-commit~=4.5
