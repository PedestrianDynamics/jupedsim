language: cpp
os: linux

branches:
  only:
    - v0.8

before_script:
    - sudo apt-get update
    - sudo apt-get install libboost-all-dev
    - sudo apt-get install cmake
# before_script:
  # - brew update
  # - brew unlink boost
  # - brew install boost
  # - brew install cmake

script:
    - cmake .. -DBUILD_TESTING=ON
    - make -j$(nproc)     
    - ctest -R Boost 

notifications:
   email:
     recipients:
       - mohcine@chraibi.de
     on_success: change
     on_failure: always

